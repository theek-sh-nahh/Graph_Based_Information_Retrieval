Supervised learning requires labeled training data. Supervised learning requires labeled training data. Search engines rely on indexing and retrieval models to process large document collections. Information retrieval systems rank documents using relevance scoring algorithms.